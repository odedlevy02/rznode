import * as sinon from "sinon";
import * as assert from "assert";

//if the call is a promise - inside 'it' return the result

describe("<%=origTestName%> tests",()=> {
  it("should work",() =>{
      assert(true)
  })
});
